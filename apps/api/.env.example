# PostgreSQL Connection String
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/aksicendekia?schema=public"

# Application Config
JWT_SECRET="supersecretkey12345"
PORT=3001
HOST="0.0.0.0"

# Feature 011 — Focus mode (specs/011-sd-math-focus)
# Mirrors apps/web's NEXT_PUBLIC_FOCUS_ENABLED. When true (default if unset),
# public/curriculum queries are restricted to stage SD + subject Matematika.
FOCUS_ENABLED=true
